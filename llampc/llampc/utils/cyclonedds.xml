<?xml version="1.0" encoding="UTF-8" ?>
<CycloneDDS xmlns="https://cdds.io/config"
            xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
            xsi:schemaLocation="https://cdds.io/config https://raw.githubusercontent.com/eclipse-cyclonedds/cyclonedds/master/etc/cyclonedds.xsd">
  <Domain Id="any">
    <General>
      <!-- Pin to the WiFi interface so DDS doesn't try docker0/lo/eth. -->
      <!-- Use the interface NAME (e.g. wlan0) or the machine's own IP. -->
      <Interfaces>
        <NetworkInterface name="wlan0" priority="default" multicast="false"/>
      </Interfaces>
      <AllowMulticast>false</AllowMulticast>
    </General>

    <Discovery>
      <!-- No multicast discovery; list every machine's IP explicitly. -->
      <ParticipantIndex>auto</ParticipantIndex>
      <Peers>
        <Peer Address="192.168.1.10"/>   <!-- car -->
        <Peer Address="192.168.1.20"/>   <!-- laptop -->
      </Peers>
      <!-- Faster re-discovery after a dropout on a flaky link. -->
      <MaxAutoParticipantIndex>20</MaxAutoParticipantIndex>
    </Discovery>

    <Internal>
      <!-- Don't let one congested writer stall others. -->
      <Watermarks>
        <WhcHigh>500kB</WhcHigh>
      </Watermarks>
    </Internal>
  </Domain>
</CycloneDDS>


<!-- export RMW_IMPLEMENTATION=rmw_cyclonedds_cpp
export CYCLONEDDS_URI=file:///path/to/cyclonedds.xml
export ROS_DOMAIN_ID=0          # same number on both -->